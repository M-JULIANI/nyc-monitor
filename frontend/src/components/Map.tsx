const Map = () => {
  return (
    <div style={{ width: '80vw', height: '60vh', background: '#e0e0e0', display: 'flex', alignItems: 'center', justifyContent: 'center', borderRadius: '8px' }}>
      {/* TODO: Add Mapbox searchbox and map using react-map-gl */}
      <span>Map will appear here (Mapbox integration coming soon)</span>
    </div>
  );
};

export default Map; 